$exts = @('.go','.mod','.sql','.env','.toml','.html'); $skip = @('tmp','node_modules','.git','public','bucket'); Get-ChildItem -Recurse -File | Where-Object { $exts -contains $_.Extension -and -not ($skip | Where-Object { $_.FullName -match "\\$_\\.+" }) } | ForEach-Object { "`n===== $($_.FullName.Replace((Get-Location).Path + '\','')) =====`n"; Get-Content $_.FullName -Raw } | Set-Content output.txt